#
# course - Pylons configuration
#
# The %(here)s variable will be replaced with the parent directory of this file
#
[DEFAULT]
debug = false
email_to = peter@localhost
smtp_server = localhost
error_email_from = course@localhost

[server:main]
use = egg:Paste#http
#use = egg:PasteScript#flup_scgi_thread 
#host = localhost
host = 0.0.0.0 
port = 5000 

[app:main]
use = egg:course
full_stack = true
cache_dir = %(here)s/data
beaker.session.key = course 
beaker.session.secret = uzC7ejDOMai5Jd2T3hICtRmel
app_instance_uuid = {83061db8-81c0-4621-be40-9dd360c23b35}
sqlalchemy.url = sqlite:////var/tmp/math0010.sqlite
course.msg_dir = %(here)s/private/messages
course.downloads_dir = %(here)s/private/downloads
mako_templates = %(here)s/templates
static_files = %(here)s/public
filter-with = proxy-prefix

[filter:proxy-prefix]
use = egg:PasteDeploy#prefix
prefix = /math0010

# If you'd like to fine-tune the individual locations of the cache data dirs
# for the Cache data, or the Session saves, un-comment the desired settings
# here:
#beaker.cache.data_dir = %(here)s/data/cache
#beaker.session.data_dir = %(here)s/data/sessions

# WARNING: *THE LINE BELOW MUST BE UNCOMMENTED ON A PRODUCTION ENVIRONMENT*
# Debug mode will enable the interactive debugging tool, allowing ANYONE to
# execute malicious code after an exception is raised.
set debug = false


# Logging configuration
[loggers]
keys = root

[handlers]
keys = syslog

[formatters]
keys = generic

[logger_root]
level = INFO
handlers = syslog

[handler_syslog]
class=handlers.SysLogHandler
level = INFO 
formatter=generic
args=('/dev/log', handlers.SysLogHandler.LOG_USER)

[formatter_generic]
format = %(asctime)s %(levelname)-5.5s [%(name)s] %(message)s
